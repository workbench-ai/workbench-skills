# Artifact Evaluation Prompt

Create an output artifact that satisfies the uploaded case evidence and rubric.

For the starter smoke case, `output.txt` is the candidate output. Replace this prompt and the output workflow with the real artifact generation or transformation process before using the eval as a benchmark.
