# Expected Evidence

Place goldens or extracted evidence here, such as `golden.docx`, `golden.xlsx`, `golden.pdf`, or `evidence.json`.

The starter `text.txt` file is intentionally non-empty so the smoke evaluator cannot pass without real expectations. Replace it with artifact-derived evidence for the workflow being evaluated.
